# Rename this file to .env and replace the placeholder with your actual API key.
# IMPORTANT: Make sure .env is listed in your .gitignore file to avoid committing secrets!

# Google Generative AI API Key (Required for script generation)
# Get your key from Google AI Studio: https://aistudio.google.com/app/apikey
GOOGLE_GENAI_API_KEY=YOUR_GOOGLE_GENAI_API_KEY_HERE

# Optional: Allowed origins for CORS (Comma-separated list or '*')
# Defaults to '*' if not set. For production, list specific domains.
# ALLOWED_ORIGIN=https://yourdomain.com,https://yourotherdomain.com

# Optional: Allowed origins for embedding in an iframe (Space-separated list or '*')
# Defaults to '*' if not set. For production, list specific domains.
# ALLOWED_EMBEDDING_ORIGIN='self' https://yourdomain.com https://partner.com
